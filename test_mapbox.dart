import 'package:mapbox_maps_flutter/mapbox_maps_flutter.dart';
void main() {
  // Just to see if it compiles
  MapboxMap? map;
  map?.style.setStyleImportConfigProperty('basemap', 'lightPreset', 'dark');
}
